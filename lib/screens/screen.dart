import 'package:flutter/material.dart';

class Screen extends StatelessWidget {
  static final routeName = '/lesson1';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.red,
      ),
    );
  }
}
