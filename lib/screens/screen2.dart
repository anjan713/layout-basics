import 'package:flutter/material.dart';

class Screen2 extends StatelessWidget {
  static final routeName = '/lesson2';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(color: Colors.red, height: 150, width: 150),
    );
  }
}
