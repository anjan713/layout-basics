import 'package:flutter/material.dart';

class Screen6 extends StatelessWidget {
  static final routeName = '/lesson6';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
          child: Container(
        color: Colors.red,
      )),
    );
  }
}
