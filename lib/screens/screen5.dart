import 'package:flutter/material.dart';

class Screen5 extends StatelessWidget {
  static final routeName = '/lesson5';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
          child: Container(
              color: Colors.red,
              height: double.infinity,
              width: double.infinity)),
    );
  }
}
